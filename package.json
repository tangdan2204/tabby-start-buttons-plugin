{
  "name": "tabby-start-buttons",
  "version": "0.8.1",
  "description": "Agent Mux - AI Agent Terminal Enhancement (Codex CLI / Claude Code)",
  "main": "dist/index.js",
  "keywords": [
    "tabby-plugin"
  ],
  "author": "tangdan01",
  "license": "MIT",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "deploy": "node scripts/deploy.js",
    "deploy:quick": "node scripts/deploy.js --skip-build",
    "release": "npm version patch && npm run deploy",
    "release:minor": "npm version minor && npm run deploy"
  },
  "peerDependencies": {
    "tabby-core": "*",
    "tabby-terminal": "*",
    "@angular/core": ">=14",
    "rxjs": ">=7"
  },
  "devDependencies": {
    "typescript": "~5.4.0",
    "webpack": "^5.90.0",
    "webpack-cli": "^5.1.0",
    "ts-loader": "^9.5.0"
  }
}